# Deliberate hardware-reset evidence image: after 10 s of normal runtime the
# safety supervisor stops feeding and the 5 s nominal IWDG must reset the MCU.
CONFIG_AMS_IWDG_VALIDATION_MODE=y
CONFIG_AMS_WATCHDOG_TARGET_VALIDATED=n
CONFIG_AMS_IWDG_STOP_FEED_TEST_AFTER_MS=10000
